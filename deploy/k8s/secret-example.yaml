# Generated By: Claude Code (Claude Opus 4.7 (1M context))
#
# EXAMPLE ONLY. Do not commit real credentials. Mint a token with
# `make mint-creds` (locally), then either:
#   kubectl create secret generic claude-quota-exporter-credentials \
#       --from-file=credentials.json=./mint/.credentials.json
# or replace the base64 payload below with `base64 -w0 < ./mint/.credentials.json`.
#
# Expected shape of the decoded file:
#   {
#     "claudeAiOauth": {
#       "accessToken": "sk-ant-oat01-...",
#       "expiresAt": 1811116800000
#     }
#   }
#
# expiresAt is Unix milliseconds since epoch (informational only —
# the exporter does not refresh; operators alert on
# claude_quota_access_token_expires_at_seconds approaching now).
apiVersion: v1
kind: Secret
metadata:
  name: claude-quota-exporter-credentials
type: Opaque
data:
  # Base64 of:
  # {"claudeAiOauth":{"accessToken":"REPLACE_ME","expiresAt":0}}
  credentials.json: eyJjbGF1ZGVBaU9hdXRoIjp7ImFjY2Vzc1Rva2VuIjoiUkVQTEFDRV9NRSIsImV4cGlyZXNBdCI6MH19
