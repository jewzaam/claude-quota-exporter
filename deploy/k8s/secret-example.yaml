# Generated By: Claude Code (Claude Opus 4.7 (1M context))
#
# EXAMPLE ONLY. Do not commit real credentials. Build the Secret from
# a PKCE-login-derived OAuth bearer (the access token stored in
# ~/.claude/.credentials.json by `claude /login`). Rotation is owned
# by an external workload; the exporter re-reads this Secret on every
# fetch attempt, so updates are picked up on the next scrape with no
# pod restart.
#
# Build with:
#   echo '{"claudeAiOauth":{"accessToken":"sk-ant-oat01-..."}}' \
#     | kubectl create secret generic claude-quota-exporter-credentials \
#         --from-file=credentials.json=/dev/stdin --dry-run=client -o yaml
#
# Expected shape of the decoded file:
#   {"claudeAiOauth": {"accessToken": "sk-ant-oat01-..."}}
apiVersion: v1
kind: Secret
metadata:
  name: claude-quota-exporter-credentials
type: Opaque
data:
  # Base64 of:
  # {"claudeAiOauth":{"accessToken":"REPLACE_ME"}}
  credentials.json: eyJjbGF1ZGVBaU9hdXRoIjp7ImFjY2Vzc1Rva2VuIjoiUkVQTEFDRV9NRSJ9fQ==
