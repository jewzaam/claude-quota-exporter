# Generated By: Claude Code (Claude Opus 4.7 (1M context))
apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude-quota-exporter
  labels:
    app.kubernetes.io/name: claude-quota-exporter
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: claude-quota-exporter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: claude-quota-exporter
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: exporter
          image: claude-quota-exporter:0.2.0
          imagePullPolicy: IfNotPresent
          args:
            - --config
            - /etc/claude-quota-exporter/config.json
          ports:
            - name: metrics
              containerPort: 9180
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: config
              mountPath: /etc/claude-quota-exporter
              readOnly: true
            - name: credentials
              mountPath: /var/run/claude
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: claude-quota-exporter-config
        - name: credentials
          secret:
            secretName: claude-quota-exporter-credentials
